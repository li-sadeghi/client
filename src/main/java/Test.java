import network.offline.LoadMessage;
import network.offline.OfflineMessage;
import network.offline.SaveMessage;

import java.util.ArrayList;
import java.util.List;

public class Test {
    public static void main(String[] args) {
//        List<OfflineMessage> messages = LoadMessage.fetchAll(OfflineMessage.class);
//        for (OfflineMessage message : messages) {
//            if (message.getSenderUsername().equals("123")){
//                LoadMessage.delete(message);
//            }
//        }

    }
}
